# Роль: Главный администратор (Admin RB)

## Описание роли

Роль **Главный администратор** (Admin RB) предназначена для пользователей, которые управляют всей системой. Главный администратор имеет полный доступ ко всем функциям системы, включая управление пользователями, организациями, модерацию и просмотр логов.

## Получение роли

- **Способ получения:** Назначается вручную администратором системы
- **Требования:** 
  - Пользователь должен быть суперпользователем Django (`is_superuser=True`)
  - Роль `admin_rb` должна быть создана в системе
- **Доступность:** Только для назначения администратором

## Данные для входа

### Главный администратор по умолчанию:

**Email:** `admin@admin.ru`  
**Пароль:** `qwqwqw12`

⚠️ **ВАЖНО:** После первого входа рекомендуется изменить пароль!

### Создание главного администратора:

```bash
python manage.py create_main_admin
```

Эта команда создаст или обновит главного администратора с указанными выше данными.

### Вход:
1. Перейти на страницу входа: `/login/`
2. Ввести email и пароль
3. После входа переключиться на роль `admin_rb` (если есть несколько ролей)

## Функционал

### 1. Личный кабинет

**URL:** `/dashboard/` (при активной роли `admin_rb`)

#### 1.1. Общие данные
- ФИО (синхронизируется со всеми ролями)
- Фото профиля (синхронизируется)
- Email
- Телефон
- Город (синхронизируется)

#### 1.2. Статистика системы
- Общее количество пользователей
- Количество пользователей по ролям
- Количество организаций (одобренных, ожидающих, отклонённых)
- Количество мероприятий
- Статистика посещаемости
- Активность за последние 30 дней

### 2. Управление пользователями

**Раздел:** "Пользователи"

#### Возможности:
- Просмотр всех пользователей системы
- Поиск пользователей по email, имени, фамилии
- Фильтрация по ролям
- Просмотр данных пользователя:
  - Email, ФИО, телефон
  - Роли пользователя
  - Статус активности
  - Дата регистрации
- Активация/деактивация пользователей
- Просмотр профилей пользователей

#### API:
- `GET /api/admin-rb/users/` - Список пользователей
- `GET /api/admin-rb/users/<id>/` - Данные пользователя
- `POST /api/admin-rb/users/<id>/toggle-status/` - Изменить статус пользователя

### 3. Управление организациями

**Раздел:** "Организации"

#### Возможности:
- Просмотр всех организаций
- Поиск организаций по названию, ИНН
- Фильтрация по статусу (одобрены, ожидают, отклонены)
- Просмотр данных организации:
  - Название, ИНН, адрес
  - Город
  - Статус
  - Создатель
  - Дата создания
- Просмотр детальной информации об организации

#### API:
- `GET /api/admin-rb/organizations/` - Список организаций
- `GET /api/admin-rb/organizations/pending/` - Организации на модерации

### 4. Модерация организаций

**Раздел:** "Модерация"

#### Возможности:
- Просмотр организаций, ожидающих модерации
- Просмотр всех данных организации:
  - Название, тип, ИНН
  - Город, адрес, координаты
  - Документы (лицензия, устав)
  - Создатель организации
- **Одобрение организации:**
  - Проверка данных
  - Одобрение организации
  - Автоматическое назначение роли `director` создателю
  - Отправка уведомления создателю
- **Отклонение организации:**
  - Указание причины отклонения
  - Отправка уведомления создателю

#### Процесс модерации:
1. Пользователь создаёт организацию
2. Организация получает статус `pending`
3. Администратор проверяет данные
4. Администратор одобряет или отклоняет
5. При одобрении:
   - Статус меняется на `approved`
   - Создателю присваивается роль `director`
   - Организация становится видимой в каталоге

#### API:
- `PATCH /api/organizations/<id>/moderate/` - Одобрить/отклонить организацию

### 5. Управление кодами регистрации спорткомитета

#### Возможности:
- Создание кодов регистрации для сотрудников спорткомитета
- Просмотр всех кодов
- Фильтрация по статусу (использован/не использован)
- Просмотр информации о коде:
  - Код
  - Город
  - Отдел, должность
  - Кем выдан
  - Статус использования
  - Кто использовал и когда

#### Создание кодов:
- Через админ-панель: `/admin/city_committee/committeeregistrationcode/add/`
- Через команду: `python manage.py create_committee_code --city "Уфа" --count 5`

### 6. Просмотр логов системы

**Раздел:** "Логи системы"

#### Возможности:
- Просмотр аудит логов:
  - Дата/время
  - Пользователь
  - Действие
  - IP адрес
  - Детали
- Просмотр Django логов:
  - Логи приложения
  - Ошибки и предупреждения

#### API:
- `GET /api/admin-rb/logs/` - Получить логи системы

### 7. Управление ролями пользователей

#### Возможности:
- Просмотр всех ролей пользователя
- Назначение ролей пользователям (через админ-панель Django)
- Деактивация ролей

### 8. Доступ к админ-панели Django

**URL:** `/admin/`

#### Возможности:
- Полный доступ к админ-панели Django
- Управление всеми моделями системы
- Просмотр и редактирование данных
- Управление пользователями и правами

### 9. Уведомления

**Раздел:** "Уведомления"

#### Типы уведомлений:
- Заявки на создание организаций
- Системные уведомления
- Сообщения от пользователей

### 10. Быстрые действия

- ✏️ Редактировать общие данные → `/users/basic-data/edit/`
- 👥 Пользователи → `/dashboard/` (раздел "Пользователи")
- 🏢 Организации → `/dashboard/` (раздел "Организации")
- ✅ Модерация → `/dashboard/` (раздел "Модерация")
- 📋 Логи → `/dashboard/` (раздел "Логи системы")
- ⚙️ Админ-панель → `/admin/`

## API Endpoints

### Статистика
- `GET /api/admin-rb/stats/` - Общая статистика системы

### Пользователи
- `GET /api/admin-rb/users/` - Список пользователей
- `GET /api/admin-rb/users/<id>/` - Данные пользователя
- `POST /api/admin-rb/users/<id>/toggle-status/` - Изменить статус

### Организации
- `GET /api/admin-rb/organizations/` - Список организаций
- `GET /api/admin-rb/organizations/pending/` - Организации на модерации

### Логи
- `GET /api/admin-rb/logs/` - Логи системы

### Модерация
- `PATCH /api/organizations/<id>/moderate/` - Одобрить/отклонить организацию

## Ограничения

1. **Просмотр профилей:** Может просматривать только профили спортсменов и тренеров (не может просматривать профили других админов, кроме своего)
2. **Безопасность:** Не может просматривать свой профиль другими пользователями (только сам)

## Особенности

- **Полный доступ:** Имеет полный доступ ко всем функциям системы
- **Модерация:** Единственная роль, которая может модерировать организации
- **Управление кодами:** Может создавать коды регистрации для сотрудников спорткомитета
- **Логи:** Имеет доступ к логам системы

## Безопасность

- Главный администратор должен иметь сложный пароль
- Рекомендуется использовать двухфакторную аутентификацию
- Регулярно проверять логи системы
- Не передавать данные для входа третьим лицам

## Создание главного администратора

### Через команду:
```bash
python manage.py create_main_admin
```

### Через Django shell:
```python
from django.contrib.auth import get_user_model
from apps.users.models import UserRole

User = get_user_model()
admin = User.objects.create_superuser(
    email='admin@admin.ru',
    password='qwqwqw12',
    first_name='Главный',
    last_name='Администратор'
)

UserRole.objects.create(
    user=admin,
    role='admin_rb',
    is_active=True
)
```

## Управление через админ-панель

Главный администратор имеет полный доступ к админ-панели Django по адресу `/admin/`, где может:
- Управлять всеми моделями системы
- Создавать и редактировать пользователей
- Управлять ролями
- Просматривать и редактировать все данные
