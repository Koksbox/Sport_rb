# Changelog: Разделение API и Frontend

## Дата: 2025-01-19

### Изменения

Проект разделен на два отдельных приложения: `apps/api` и `apps/frontend`.

### Созданные файлы

#### API приложение
- `apps/api/__init__.py` - инициализация модуля
- `apps/api/apps.py` - конфигурация приложения
- `apps/api/urls.py` - централизованный роутер для всех API endpoints
- `apps/api/settings.py` - API-специфичные настройки
- `apps/api/README.md` - документация API приложения

#### Frontend приложение
- `apps/frontend/settings.py` - Frontend-специфичные настройки
- `apps/frontend/README.md` - документация Frontend приложения

#### Документация
- `README_API_SEPARATION.md` - подробное описание разделения
- `CHANGELOG_API_SEPARATION.md` - этот файл

### Измененные файлы

#### `config/urls.py`
**Было:**
```python
urlpatterns = [
    path('', include('apps.frontend.urls')),
    path('api/auth/', include('apps.authn.api.urls')),
    path('api/users/', include('apps.users.api.urls')),
    # ... множество отдельных API маршрутов
]
```

**Стало:**
```python
urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/', include('apps.api.urls')),      # Все API в одном месте
    path('', include('apps.frontend.urls')),     # Frontend
]
```

#### `config/settings/base.py`
**Добавлено:**
```python
INSTALLED_APPS = [
    # ... существующие приложения
    'apps.api',        # Централизованное API приложение
    'apps.frontend',   # Frontend приложение
]
```

### Структура URL

#### До разделения
- API endpoints были разбросаны по разным местам в `config/urls.py`
- Сложно было понять структуру API
- Сложно было вынести API на отдельный сервер

#### После разделения
- Все API под `/api/` через `apps.api.urls`
- Все Frontend страницы под `/` через `apps.frontend.urls`
- Четкое разделение ответственности
- Легко вынести API на отдельный сервер

### Преимущества

1. **Четкое разделение**
   - API и Frontend логически разделены
   - Легче понять структуру проекта

2. **Масштабируемость**
   - API можно вынести на отдельный сервер
   - Frontend можно заменить на SPA без изменения API

3. **Упрощение разработки**
   - Разные команды могут работать независимо
   - Четкие границы между компонентами

4. **Гибкость развертывания**
   - Можно деплоить API и Frontend отдельно
   - Разные настройки для разных окружений

### Обратная совместимость

✅ **Все существующие URL работают без изменений:**
- `/api/auth/` - работает
- `/api/organizations/` - работает
- `/api/events/` - работает
- `/` - работает
- `/dashboard/` - работает

### Миграция

**Никаких миграций базы данных не требуется!**

Только изменения в структуре URL маршрутизации.

### Тестирование

Проверьте работоспособность:

```bash
# API endpoints
curl http://localhost:8000/api/core/health/
curl http://localhost:8000/api/organizations/

# Frontend страницы
curl http://localhost:8000/
curl http://localhost:8000/login/
```

### Документация

Подробная документация:
- `README_API_SEPARATION.md` - общее описание разделения
- `apps/api/README.md` - документация API приложения
- `apps/frontend/README.md` - документация Frontend приложения

### Следующие шаги

1. ✅ Разделение завершено
2. 🔄 Можно добавить версионирование API (`/api/v1/`, `/api/v2/`)
3. 🔄 Можно добавить Swagger/OpenAPI документацию
4. 🔄 Можно настроить отдельное развертывание API и Frontend
